/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["media.graphassets.com"], //Domain of image host
  },
};

module.exports = nextConfig;
